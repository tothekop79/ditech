# Frontend

Placeholder. Build with React + Vite + React Query + dnd-kit + Tailwind.

Reference the prototype HTML/JS code in the previous chat artifacts to port views:
- Calendar (with drag-drop)
- Gantt (multi-day bars)
- Map (Google Maps integration)
- Capacity (heatmap + utilization)
- Alerts (4 conflict types)
- Import (drag-drop file upload)
- Reports (executive summary)
- Notify (rule toggles)

API endpoints are at http://localhost:5000/api/*
See `docs/API.md` for full reference.
